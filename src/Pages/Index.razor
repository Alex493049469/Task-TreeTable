@page "/"
@using TreeTableTest.Data

<PageTitle>Tree Table Test</PageTitle>

<div id="tree-table">
    <TreeTableComponent Table="@table" />
</div>

<div style="position: absolute; right: 0; top: 0">
    <label for="rowIndex">Row Index</label>
    <input type="number" id="rowIndex" @bind-value="@rowIndex">

    <label for="colIndex">Column Index</label>
    <input type="number" id="colIndex" @bind-value="@colIndex">

    <button @onclick="addColumn">Add Column</button>
    <button @onclick="addRow">Add Row</button>
</div>

@code{

    TreeTable table = TableTreeDefault.DefaultTreeTable;

    int rowIndex = 1;
    int colIndex = 2;

    public void addColumn()
    {
        /**
        * Magic is supposed to happen here...
        */
    }

    public void addRow()
    {
        /**
        * Magic is supposed to happen here...
        */
    }

    protected override void OnInitialized()
    {
        table = TableTreeDefault.DefaultTreeTable;
        StateHasChanged();
    }
}